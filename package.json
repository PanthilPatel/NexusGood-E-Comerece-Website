{
  "name": "shopelite-root",
  "version": "1.0.0",
  "description": "ShopElite E-Commerce Application",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix admin",
    "dev": "concurrently -n \"API,STORE,ADMIN\" -c \"blue,green,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:admin\"",
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend",
    "install:admin": "npm install --prefix admin",
    "install:all": "npm install && npm run install:backend && npm run install:frontend && npm run install:admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
